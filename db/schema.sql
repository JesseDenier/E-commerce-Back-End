-- This is only run on the first instance of the project being used.

-- Deleted the database if a current one already exists with this name.
DROP DATABASE IF EXISTS ecommerce_db;

-- Creates a new database.
CREATE DATABASE ecommerce_db;

